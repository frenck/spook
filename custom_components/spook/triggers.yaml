cron:
  fields:
    schedule:
      required: true
      example: "0 7 * * 1-5"
      selector:
        text:

stale:
  target:
    entity:
  fields:
    for:
      required: true
      example: "01:00:00"
      selector:
        duration:

integration_failed:
  fields:
    for:
      required: true
      example: "00:15:00"
      selector:
        duration:
    entry_id:
      required: false
      selector:
        config_entry:
      multiple: true
